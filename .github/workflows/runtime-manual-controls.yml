name: Runtime Manual Controls Archive

on:
  workflow_dispatch:
    inputs:
      note:
        description: 'Archive placeholder only'
        required: false
        type: string

jobs:
  noop:
    name: Archive placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Print archive note
        run: echo "Duplicate manual controls archived. Use Runtime API Deploy."
