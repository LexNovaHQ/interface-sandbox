name: Capture CO-E2E-01-03 Phase 3 failure

on:
  pull_request:
    branches: [domain-gate-v0-preflight]
    types: [synchronize]

permissions:
  contents: read

jobs:
  capture:
    if: github.head_ref == 'co-e2e-01-03-critical-authority'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: diligence-artifact-backend
    steps:
      - uses: actions/checkout@v4
        with:
          ref: co-e2e-01-03-critical-authority
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: diligence-artifact-backend/package-lock.json
      - run: npm ci
      - name: Capture Phase 3 suite
        run: |
          set +e
          npm run check:phase3-domain-derivation > phase3-debug.log 2>&1
          code=$?
          cat phase3-debug.log
          echo "$code" > phase3-debug.exit
          exit 0
      - uses: actions/upload-artifact@v4
        with:
          name: co-e2e-phase3-debug
          path: |
            diligence-artifact-backend/phase3-debug.log
            diligence-artifact-backend/phase3-debug.exit
