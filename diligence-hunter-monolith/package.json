{
  "name": "interface-diligence-hunter-monolith",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Hunter-style single-call monolithic diligence runner for Interface Diligence.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check:syntax": "node --check server.js && node --check renderer.js && node --check vault-bridge.js && node --check scripts/static-check.mjs",
    "check": "npm run check:syntax && node scripts/static-check.mjs"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=20"
  }
}
