const ARCHETYPES = new Set(["UNI", "DOE", "JDG", "CMP", "CRT", "RDR", "ORC", "TRN", "SHD", "OPT", "MOV"]);
const SURFACES = new Set(["Consumer-Public", "Enterprise-Private", "PII", "Employment", "Sensitive/Biometric", "Financial", "Content&IP", "Safety&Physical", "Infrastructure", "Minors"]);
const CONFIDENCE = new Set(["high", "medium", "low", "unknown"]);
const TRI_STATE = new Set(["true", "false", "unknown"]);
const FORBIDDEN_KEYS = new Set(["target_profile", "primary_product", "raw_feature_candidates", "feature_map_scratchpad", "company_name", "website", "legal_entity", "hq_jurisdiction", "actual_processing_location", "data_sovereignty_signature", "legal_stack_review", "document_stack_redline", "legal_stack_assessment", "registry_ledger", "registry_evaluation", "final_status", "controlled_rows", "insufficient_evidence_rows", "operator_challenge", "report_data", "technical_audit_log", "assembly_route", "vault_confirmation_questions", "vault_prefill_suggestions", "vault_payload", "handoff_envelope", "assembly_handoff", "html"]);
const THREAT_STATUSES = new Set(["TRIGGERED", "CONTROLLED", "NOT_TRIGGERED", "NOT_APPLICABLE", "INSUFFICIENT_EVIDENCE"]);
function issue(keyword, severity, instancePath, message, params = {}) { return { keyword, severity, instancePath, schemaPath: "#/targetFeatureProfileGuardrails", message, params }; }
function pushBlocking(errors, path, message, params = {}) { errors.push(issue("target_feature_profile_guardrail", "BLOCKING", path, message, params)); }
function pushRepair(repairs, path, action, params = {}) { repairs.push({ severity: "REPAIRABLE", instancePath: path, action, ...params }); }
function writeLimitation(profile, warning) { if (!profile || typeof profile !== "object") return; if (!Array.isArray(profile.limitations)) profile.limitations = []; const line = `GUARDRAIL_WARNING ${warning.instancePath || "/"}: ${warning.message} ${JSON.stringify(warning.params || {})}`; if (!profile.limitations.includes(line)) profile.limitations.push(line); }
function warn(profile, warnings, path, message, params = {}) { const item = issue("target_feature_profile_guardrail_warning", "WARNING", path, message, params); warnings.push(item); writeLimitation(profile, item); }
function nonEmptyString(value) { return typeof value === "string" && value.trim().length > 0; }
function normalizeUrl(value) { try { const url = new URL(/^https?:\/\//i.test(String(value || "")) ? value : `https://${value}`); url.hash = ""; url.search = ""; if ((url.pathname || "") !== "/") url.pathname = url.pathname.replace(/\/+$/, "") || "/"; return url.toString(); } catch { return String(value || "").trim(); } }
function hostnameFor(value) { try { const url = new URL(/^https?:\/\//i.test(String(value || "")) ? value : `https://${value}`); return url.hostname.toLowerCase().replace(/^www\./, ""); } catch { return ""; } }
function addDomain(domains, value) { const host = hostnameFor(value); if (host && host !== "unknown") domains.add(host); }
function collectPackageDomains(packageInput = {}) { const domains = new Set(); addDomain(domains, packageInput?.source_bundle?.target_input?.primary_url); addDomain(domains, packageInput?.source_bundle?.target_input?.registrable_domain); addDomain(domains, packageInput?.target_profile_v2?.identity?.domain); addDomain(domains, packageInput?.target_profile_v2?.identity?.website); for (const record of packageInput?.source_bundle?.evidence_buffer || []) { addDomain(domains, record?.source_url); addDomain(domains, record?.final_url); addDomain(domains, record?.url); } return domains; }
function isFirstPartyUrl(sourceUrl, domains) { const host = hostnameFor(sourceUrl); if (!host) return false; for (const domain of domains) if (host === domain || host.endsWith(`.${domain}`) || domain.endsWith(`.${host}`)) return true; return false; }
function urlKeys(record = {}) { return [record.source_url, record.final_url, record.url, record.feature_source_url, record.document_url].filter(nonEmptyString).flatMap((v) => [String(v).trim(), normalizeUrl(v)]); }
function refList(value) { return Array.isArray(value) ? value.map((x) => String(x || "").trim()).filter(Boolean) : (nonEmptyString(value) ? [String(value).trim()] : []); }
function harvestPackageSources(value, out = [], seen = new WeakSet()) { if (!value || typeof value !== "object") return out; if (seen.has(value)) return out; seen.add(value); if (!Array.isArray(value)) { for (const key of ["source_url", "final_url", "url", "feature_source_url", "document_url"]) { if (nonEmptyString(value[key])) { out.push({ source_url: value.source_url || value.url || value.feature_source_url || value.document_url || value[key], final_url: value.final_url || null, evidence_refs: value.evidence_refs || [], source_citation_manifest: value.source_citation_manifest || value.evidence_ref_manifest || [] }); break; } } } for (const child of Array.isArray(value) ? value : Object.values(value)) harvestPackageSources(child, out, seen); return out; }
function buildEvidenceIndex(evidenceBuffer = [], packageInput = {}) { const byUrl = new Map(); const refs = new Set(); const addRef = (ref) => { if (nonEmptyString(ref)) refs.add(String(ref).trim()); }; const addRecordRefs = (record = {}) => { for (const ref of refList(record.evidence_refs)) addRef(ref); for (const m of [...(Array.isArray(record.source_citation_manifest) ? record.source_citation_manifest : []), ...(Array.isArray(record.evidence_ref_manifest) ? record.evidence_ref_manifest : [])]) { addRef(m.evidence_ref_id); addRef(m.evidence_source_id); addRef(m.evidence_source_id && m.chunk_id ? `${m.evidence_source_id}#${m.chunk_id}` : ""); } if (record.evidence_source_id) addRef(record.evidence_source_id); };
  const add = (record, scope) => { const normalized = { ...record, admission_scope: scope }; for (const key of urlKeys(record)) { if (!byUrl.has(key)) byUrl.set(key, []); byUrl.get(key).push(normalized); } addRecordRefs(record); };
  for (const record of Array.isArray(evidenceBuffer) ? evidenceBuffer : []) add(record, "stage_evidence_buffer"); for (const record of harvestPackageSources(packageInput)) add(record, "package_source"); for (const ref of packageInput?.source_bundle?.source_citation_manifest || []) { addRef(ref.evidence_ref_id); addRef(ref.evidence_source_id); addRef(ref.evidence_source_id && ref.chunk_id ? `${ref.evidence_source_id}#${ref.chunk_id}` : ""); } for (const ref of packageInput?.source_bundle?.evidence_ref_manifest || []) { addRef(ref.evidence_ref_id); addRef(ref.evidence_source_id); addRef(ref.evidence_source_id && ref.chunk_id ? `${ref.evidence_source_id}#${ref.chunk_id}` : ""); } return { byUrl, refs, packageDomains: collectPackageDomains(packageInput) }; }
function matchingRecords(sourceUrl, evidenceIndex) { const keys = urlKeys({ source_url: sourceUrl }); const out = []; const seen = new Set(); for (const key of keys) for (const r of evidenceIndex.byUrl.get(key) || []) { const id = `${r.source_url}|${r.final_url}|${r.admission_scope}`; if (!seen.has(id)) { seen.add(id); out.push(r); } } return out; }
function validateSourceAndRefs(owner, sourceKey, refsKey, base, profile, warnings, errors, evidenceIndex) { const sourceUrl = owner?.[sourceKey]; if (!nonEmptyString(sourceUrl)) warn(profile, warnings, `${base}/${sourceKey}`, `${sourceKey} missing; passed with warning`); else { const matches = matchingRecords(sourceUrl, evidenceIndex); if (!matches.length && !isFirstPartyUrl(sourceUrl, evidenceIndex.packageDomains)) pushBlocking(errors, `${base}/${sourceKey}`, `${sourceKey} must be a first-party source admitted somewhere in the diligence package`, { source_url: sourceUrl }); else if (!matches.length) warn(profile, warnings, `${base}/${sourceKey}`, `${sourceKey} is first-party but not indexed in current package; passed with warning`, { source_url: sourceUrl }); }
  const refs = refList(owner?.[refsKey]); if (!refs.length) { warn(profile, warnings, `${base}/${refsKey}`, `${refsKey} missing; deterministic quote resolution unavailable`); return; } const unknownRefs = refs.filter((r) => !evidenceIndex.refs.has(r) && !/^SRC_\d{3}(#C\d{3})?$/.test(r)); if (unknownRefs.length) warn(profile, warnings, `${base}/${refsKey}`, `${refsKey} contains refs not found in manifest; passed with warning`, { unknown_refs: unknownRefs }); }
function stripCrossStageKeys(value, profile, warnings, repairs, path = "") { if (!value || typeof value !== "object") return; if (Array.isArray(value)) return value.forEach((x, i) => stripCrossStageKeys(x, profile, warnings, repairs, `${path}/${i}`)); for (const key of Object.keys(value)) { const p = `${path}/${key}`; if (FORBIDDEN_KEYS.has(key)) { delete value[key]; warn(profile, warnings, p, `forbidden cross-stage key stripped before guardrail validation: ${key}`, { key }); pushRepair(repairs, p, "stripped_forbidden_cross_stage_key", { key }); continue; } if (typeof value[key] === "string" && THREAT_STATUSES.has(value[key].trim())) warn(profile, warnings, p, "Stage 7 registry status appeared in Stage 5 field; passed with warning", { value: value[key] }); stripCrossStageKeys(value[key], profile, warnings, repairs, p); } }
function validEnum(value, set, profile, warnings, path, label) { if (value && !set.has(value)) warn(profile, warnings, path, `${label} has invalid value; passed with warning`, { value }); }
function clearLinkedThreatIds(feature, base, profile, warnings, repairs) { if (Array.isArray(feature.linked_threat_ids) && feature.linked_threat_ids.length) { const old = [...feature.linked_threat_ids]; feature.linked_threat_ids = []; warn(profile, warnings, `${base}/linked_threat_ids`, "linked_threat_ids emitted before threat mapping; cleared and passed with warning", { linked_threat_ids: old }); pushRepair(repairs, `${base}/linked_threat_ids`, "cleared_premature_linked_threat_ids", { linked_threat_ids: old }); } }
function validateFeature(feature, index, context) { const { profile, errors, warnings, repairs, evidenceIndex } = context; const base = `/feature_inventory/${index}`; if (!feature || typeof feature !== "object" || Array.isArray(feature)) { warn(profile, warnings, base, "non-object feature dropped by canonicalizer/guardrail warning"); return false; } if (!nonEmptyString(feature.feature_id)) { feature.feature_id = `runtime_feature_${index + 1}`; warn(profile, warnings, `${base}/feature_id`, "feature_id missing; runtime-filled deterministic feature_id"); pushRepair(repairs, `${base}/feature_id`, "runtime_filled_missing_feature_id"); } if (!Array.isArray(feature.data_provenance) || !feature.data_provenance.length) { feature.data_provenance = [{ data_origin: "unknown", data_subject: "unknown", data_category: "unknown", processing_context: feature.system_action || "not visible in admitted evidence", storage_or_retention_signal: "not visible in admitted evidence", training_or_finetuning_signal: "not visible in admitted evidence", source_url: feature.feature_source_url || "", evidence_refs: refList(feature.evidence_refs), confidence: "unknown" }]; warn(profile, warnings, `${base}/data_provenance`, "missing data provenance runtime-filled; passed with warning"); pushRepair(repairs, `${base}/data_provenance`, "runtime_filled_missing_data_provenance"); } validEnum(feature.confidence, CONFIDENCE, profile, warnings, `${base}/confidence`, "feature confidence"); validEnum(feature.external_action_signal, TRI_STATE, profile, warnings, `${base}/external_action_signal`, "external_action_signal"); validateSourceAndRefs(feature, "feature_source_url", "evidence_refs", base, profile, warnings, errors, evidenceIndex); feature.data_provenance.forEach((item, i) => validateSourceAndRefs(item, "source_url", "evidence_refs", `${base}/data_provenance/${i}`, profile, warnings, errors, evidenceIndex)); const archetypeProvenance = Array.isArray(feature.archetype_provenance) ? feature.archetype_provenance : []; const surfaceProvenance = Array.isArray(feature.surface_provenance) ? feature.surface_provenance : []; for (const code of feature.archetype_codes || []) { if (!ARCHETYPES.has(code)) warn(profile, warnings, `${base}/archetype_codes`, `invalid archetype code; passed with warning: ${code}`, { code }); if (!archetypeProvenance.some((p) => p?.archetype_code === code)) warn(profile, warnings, `${base}/archetype_provenance`, `missing archetype provenance for code; passed with warning: ${code}`, { code }); } archetypeProvenance.forEach((item, i) => validateSourceAndRefs(item, "source_url", "evidence_refs", `${base}/archetype_provenance/${i}`, profile, warnings, errors, evidenceIndex)); for (const token of feature.surface_tokens || []) { if (!SURFACES.has(token)) warn(profile, warnings, `${base}/surface_tokens`, `invalid surface token; passed with warning: ${token}`, { token }); if (!surfaceProvenance.some((p) => p?.surface_token === token)) warn(profile, warnings, `${base}/surface_provenance`, `missing surface provenance for token; passed with warning: ${token}`, { token }); } surfaceProvenance.forEach((item, i) => validateSourceAndRefs(item, "source_url", "evidence_refs", `${base}/surface_provenance/${i}`, profile, warnings, errors, evidenceIndex)); clearLinkedThreatIds(feature, base, profile, warnings, repairs); return true; }
export function validateTargetFeatureProfileGuardrails(profile, options = {}) { const errors = []; const warnings = []; const repairs = []; if (!profile || typeof profile !== "object" || Array.isArray(profile)) return { ok: false, errors: [issue("target_feature_profile_guardrail", "BLOCKING", "", "target_feature_profile must be an object")], warnings, repairs }; Object.defineProperty(profile, "__errors", { value: errors, enumerable: false, configurable: true }); stripCrossStageKeys(profile, profile, warnings, repairs, ""); if (profile.feature_profile_version !== "feature_profile_v2") { const old = profile.feature_profile_version; profile.feature_profile_version = "feature_profile_v2"; warn(profile, warnings, "/feature_profile_version", "feature_profile_version repaired to feature_profile_v2", { old }); pushRepair(repairs, "/feature_profile_version", "repaired_feature_profile_version", { old }); } if (Array.isArray(profile.product_feature_map) && profile.product_feature_map.length) { const count = profile.product_feature_map.length; profile.product_feature_map = []; warn(profile, warnings, "/product_feature_map", "product_feature_map is legacy; cleared and passed with warning", { count }); pushRepair(repairs, "/product_feature_map", "cleared_legacy_product_feature_map", { count }); } const evidenceIndex = buildEvidenceIndex(options.evidenceBuffer || [], options.packageInput || {}); const features = Array.isArray(profile.feature_inventory) ? profile.feature_inventory : []; if (!features.length) pushBlocking(errors, "/feature_inventory", "feature_inventory must contain at least one canonical atomic feature"); features.forEach((feature, index) => validateFeature(feature, index, { profile, errors, warnings, repairs, evidenceIndex })); delete profile.__errors; return { ok: errors.length === 0, errors, warnings, repairs }; }
