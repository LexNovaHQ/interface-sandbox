# Archived M7_M8 Reference

`M7_M8` is retired from the live backend.

This archive exists only for future reference/support. Live execution must use:

1. `M7_TARGET_PROFILE`
2. `M8_TARGET_FEATURE_PROFILE`

No backend route, phase contract, validator, OpenAPI schema, or runtime packet may reintroduce a combined `M7_M8` phase.
