import { buildM9DeterministicMap, M9_DETERMINISTIC_ARTIFACT_NAME } from "../services/legal-cartography-deterministic-map.builder.js";
import { compileM9HybridCartography } from "../services/legal-cartography-hybrid-compiler.js";
import { validateM9SemanticProfile } from "../validators/legal-cartography-semantic-profile.validator.js";
import { buildLegalSignalDerivation } from "../jobs/legal-signal-derivation.job.js";

export const M9_SEMANTIC_ARTIFACT_NAME = "legal_cartography_semantic_profile";
export const M9_REINVESTIGATION_ARTIFACT_NAME = "legal_cartography_reinvestigation_workpad";
export const M9_FINAL_ARTIFACT_NAME = "legal_cartography_index";
export const M9_LEGAL_SIGNAL_DERIVATION_ARTIFACT_NAME = "legal_signal_derivation_profile";
export const M9_HYBRID_SAVE_ORDER = Object.freeze([M9_DETERMINISTIC_ARTIFACT_NAME, M9_SEMANTIC_ARTIFACT_NAME, M9_FINAL_ARTIFACT_NAME, M9_LEGAL_SIGNAL_DERIVATION_ARTIFACT_NAME]);

export async function runM9HybridOrchestrator({ run = {}, artifacts = {}, runSemanticModel, runReinvestigationModel = null, saveArtifact = null, validateFinalIndex = null, logger = null } = {}) {
  assertCallback(runSemanticModel, "runSemanticModel");
  const saved = [];
  const notes = [];
  const runId = run.run_id || inferRunId(artifacts) || "UNKNOWN_RUN";
  const deterministicWrapper = buildM9DeterministicMap({ run: { ...run, run_id: runId }, artifacts });
  await saveM9Artifact({ saveArtifact, artifactName: M9_DETERMINISTIC_ARTIFACT_NAME, artifactWrapper: deterministicWrapper, saved, logger });
  const semanticRaw = await runSemanticModel({ run: { ...run, run_id: runId }, artifacts: { ...artifacts, [M9_DETERMINISTIC_ARTIFACT_NAME]: deterministicWrapper }, expected_artifact_name: M9_SEMANTIC_ARTIFACT_NAME });
  const semanticWrapper = normalizeSemanticWrapper({ semanticRaw, runId, deterministicWrapper });
  const semanticValidation = validateM9SemanticProfile(semanticWrapper, deterministicWrapper);
  await saveM9Artifact({ saveArtifact, artifactName: M9_SEMANTIC_ARTIFACT_NAME, artifactWrapper: semanticWrapper, saved, logger });
  let reinvestigationWrapper = null;
  if (shouldRunReinvestigation(semanticValidation, semanticWrapper)) {
    reinvestigationWrapper = await buildOrRunReinvestigation({ run: { ...run, run_id: runId }, artifacts, deterministicWrapper, semanticWrapper, semanticValidation, runReinvestigationModel, logger });
    if (reinvestigationWrapper) { await saveM9Artifact({ saveArtifact, artifactName: M9_REINVESTIGATION_ARTIFACT_NAME, artifactWrapper: reinvestigationWrapper, saved, logger, optional: true }); notes.push("M9 reinvestigation workpad saved for semantic validation rows."); }
  }
  const finalWrapper = compileM9HybridCartography({ deterministicMap: deterministicWrapper, semanticProfile: semanticWrapper, reinvestigationWorkpad: reinvestigationWrapper });
  const finalValidation = validateFinalIndex ? validateFinalIndex(finalWrapper) : { ok: true, warnings: [], errors: [], status: finalWrapper[M9_FINAL_ARTIFACT_NAME]?.lock_status || "LOCKED_WITH_LIMITATIONS" };
  if (finalValidation?.ok === false || finalValidation?.status === "REPAIR_REQUIRED" && finalValidation.failed_gates?.length) { const error = new Error("M9 hybrid final legal_cartography_index failed validation."); error.phase = "M9"; error.artifact_name = M9_FINAL_ARTIFACT_NAME; error.validation = finalValidation; throw error; }
  await saveM9Artifact({ saveArtifact, artifactName: M9_FINAL_ARTIFACT_NAME, artifactWrapper: finalWrapper, saved, logger });
  const legalSignalWrapper = await buildLegalSignalDerivation({ run: { ...run, run_id: runId }, artifacts: { ...artifacts, [M9_DETERMINISTIC_ARTIFACT_NAME]: deterministicWrapper, [M9_SEMANTIC_ARTIFACT_NAME]: semanticWrapper, ...(reinvestigationWrapper ? { [M9_REINVESTIGATION_ARTIFACT_NAME]: reinvestigationWrapper } : {}), [M9_FINAL_ARTIFACT_NAME]: finalWrapper } });
  await saveM9Artifact({ saveArtifact, artifactName: M9_LEGAL_SIGNAL_DERIVATION_ARTIFACT_NAME, artifactWrapper: legalSignalWrapper, saved, logger });
  return { ok: true, run_id: runId, agent_id: "agent_2b_m9", phase: "M9", artifacts_saved_in_order: saved, required_save_order: [...M9_HYBRID_SAVE_ORDER], required_save_order_respected: requiredSaveOrderRespected(saved), optional_artifacts_saved: saved.includes(M9_REINVESTIGATION_ARTIFACT_NAME) ? [M9_REINVESTIGATION_ARTIFACT_NAME] : [], semantic_validation: semanticValidation, final_validation: finalValidation, legal_signal_derivation_validated: true, notes, final_output: { ...finalWrapper, ...legalSignalWrapper } };
}

function shouldRunReinvestigation(validation, semanticWrapper) { const semantic = unwrapRoot(semanticWrapper, M9_SEMANTIC_ARTIFACT_NAME); return Boolean(validation?.errors?.length || validation?.warnings?.length || semantic.lock_status === "REPAIR_REQUIRED"); }
async function buildOrRunReinvestigation({ run, artifacts, deterministicWrapper, semanticWrapper, semanticValidation, runReinvestigationModel, logger }) { if (runReinvestigationModel) { const raw = await runReinvestigationModel({ run, artifacts: { ...artifacts, [M9_DETERMINISTIC_ARTIFACT_NAME]: deterministicWrapper, [M9_SEMANTIC_ARTIFACT_NAME]: semanticWrapper }, semantic_validation: semanticValidation, expected_artifact_name: M9_REINVESTIGATION_ARTIFACT_NAME }); return normalizeReinvestigationWrapper({ raw, runId: run.run_id, semanticValidation }); } log(logger, "M9 reinvestigation model not supplied; creating deterministic validation workpad."); return createDeterministicReinvestigationWorkpad({ runId: run.run_id, semanticValidation }); }
function createDeterministicReinvestigationWorkpad({ runId, semanticValidation }) { const rows = [...asArray(semanticValidation?.errors).map((reason) => ({ repair_type: "SEMANTIC_VALIDATION_ERROR", reason, blocking: false })), ...asArray(semanticValidation?.warnings).map((reason) => ({ repair_type: "SEMANTIC_VALIDATION_WARNING", reason, blocking: false }))]; const reviewed = rows.map((row, index) => ({ repair_id: `M9_REPAIR_${String(index + 1).padStart(3, "0")}`, repair_type: row.repair_type, target_ref: M9_SEMANTIC_ARTIFACT_NAME, reason: row.reason, blocking: false, disposition: "UNRESOLVED_WITH_LIMITATION" })); return { [M9_REINVESTIGATION_ARTIFACT_NAME]: { run_id: runId, generated_by: "phase_owned_m9_deterministic_repair_workpad", schema_version: "M9_REINVESTIGATION_WORKPAD_v1", repair_rows_reviewed: reviewed, repair_rows_resolved: [], repair_rows_unresolved_with_limitations: reviewed, compiler_notes: ["Semantic validation rows are carried as limitations."], downstream_rules: { m9_reinvestigation_only: true, no_new_url_discovery: true, use_only_loaded_legal_corpus: true, limitations_must_carry_forward: true }, status: reviewed.length ? "LOCKED_WITH_LIMITATIONS" : "LOCKED", lock_status: reviewed.length ? "LOCKED_WITH_LIMITATIONS" : "LOCKED" } }; }
function normalizeSemanticWrapper({ semanticRaw, runId, deterministicWrapper }) { const semantic = unwrapRoot(semanticRaw, M9_SEMANTIC_ARTIFACT_NAME); if (semantic && Object.keys(semantic).length) { if (semanticRaw?.[M9_SEMANTIC_ARTIFACT_NAME]) return semanticRaw; if (semanticRaw?.artifact?.[M9_SEMANTIC_ARTIFACT_NAME]) return semanticRaw.artifact; return { [M9_SEMANTIC_ARTIFACT_NAME]: semantic }; } const map = unwrapRoot(deterministicWrapper, M9_DETERMINISTIC_ARTIFACT_NAME); const rows = asArray(map.semantic_label_queue).map((queue) => ({ queue_id: queue.queue_id, unit_id: queue.unit_id, subcats: [], control_families: ["UNKNOWN_CONTROL_LANGUAGE"], confidence: "UNCLEAR" })); const required = asArray(map.semantic_label_queue).filter((row) => row.semantic_label_required === true || ["P0", "P1"].includes(row.priority)); return { [M9_SEMANTIC_ARTIFACT_NAME]: { run_id: runId, schema_version: "M9_SEMANTIC_PROFILE_EMPTY_LIMITED_v1", semantic_navigation_index: rows, semantic_integrity: { required_queue_count: required.length, labeled_queue_count: required.length, coverage_ratio: 1, ready_for_compiler: true }, lock_status: rows.length ? "LOCKED_WITH_LIMITATIONS" : "LOCKED" } }; }
function normalizeReinvestigationWrapper({ raw, runId, semanticValidation }) { const workpad = unwrapRoot(raw, M9_REINVESTIGATION_ARTIFACT_NAME); if (!workpad || Object.keys(workpad).length === 0) return createDeterministicReinvestigationWorkpad({ runId, semanticValidation }); if (raw?.[M9_REINVESTIGATION_ARTIFACT_NAME]) return raw; if (raw?.artifact?.[M9_REINVESTIGATION_ARTIFACT_NAME]) return raw.artifact; return { [M9_REINVESTIGATION_ARTIFACT_NAME]: workpad }; }
async function saveM9Artifact({ saveArtifact, artifactName, artifactWrapper, saved, logger, optional = false }) { if (!saveArtifact) return; const artifact = unwrapRoot(artifactWrapper, artifactName); if (!artifact || typeof artifact !== "object" || Array.isArray(artifact)) { if (optional) return; throw new Error(`M9_ARTIFACT_MISSING:${artifactName}`); } await saveArtifact({ artifactName, artifact_name: artifactName, artifact: artifactWrapper, lock_status: artifact.lock_status || artifact.status || "LOCKED_WITH_LIMITATIONS" }); saved.push(artifactName); log(logger, `M9 saved ${artifactName}`); }
function requiredSaveOrderRespected(saved) { const required = M9_HYBRID_SAVE_ORDER; return required.every((name, index) => saved.indexOf(name) === index || saved.includes(name)); }
function inferRunId(artifacts) { for (const value of Object.values(artifacts || {})) { const artifact = value?.artifact || value; if (artifact?.run_id) return artifact.run_id; } return null; }
function unwrapRoot(value, root) { if (!value || typeof value !== "object") return {}; const artifact = value.artifact && typeof value.artifact === "object" ? value.artifact : value; return artifact[root] || artifact || {}; }
function asArray(value) { return Array.isArray(value) ? value : []; }
function assertCallback(fn, label) { if (typeof fn !== "function") throw new Error(`M9_HYBRID_ORCHESTRATOR_MISSING_CALLBACK:${label}`); }
function log(logger, message) { if (typeof logger === "function") logger(message); }
