import { config, requireRuntimeConfig } from "./config.js";
import { assertRunId } from "./run-id.js";
import { assertKnownArtifactName, assertKnownPhase, assertPhaseCanWriteArtifact, M11_BATCH_ARTIFACT_PATTERN, M11_BATCH_VALIDATION_ARTIFACT_PATTERN } from "./constants.js";
import { assertCanReadArtifact, assertCanWriteArtifact } from "./permissions.js";
import { saveArtifactSchema, lockPhaseSchema, parseOrThrow } from "./schemas.js";
import { saveJsonArtifactToDrive, readJsonArtifactFromDrive } from "./drive.js";
import { updateRunDashboardRow } from "./sheets.js";
import { getRunRecord, updateRunRecord, getNextArtifactVersion, saveArtifactMetadata, getArtifactMetadata, listArtifactMetadata, logEvent } from "./firestore.js";
import { getRequiredWritesForPhase } from "./phase-contracts.js";
import { mergeUploadedDocumentSourcesIntoArtifact } from "./document-source-ingestor.js";
import { isStaleDeterministicForensics } from "./deterministic-profile-forensics.js";
import { buildExtendedDapIndiaReadinessProfile } from "./extended-dap-india-readiness.js";

const LOCK_ADVANCE_STATUSES = new Set(["LOCKED", "LOCKED_WITH_LIMITATIONS", "COMPLETE"]);
const ACCEPTED_PHASE_STATUSES = new Set(["LOCKED", "LOCKED_WITH_LIMITATIONS"]);
const M11_FORENSIC_CONTRACT = "M11_ROW_LEVEL_TRACE_CONTRACT_V1";
const DETERMINISTIC_PROFILE_FORENSICS = new Set(["target_profile_forensics", "target_feature_profile_forensics", "data_provenance_profile_forensics", "exposure_registry_profile_forensics"]);
const AGENT_4B_ARTIFACT = "extended_dap_india_readiness_profile";
const AGENT_4B_PHASE = "AGENT_4B_EXTENDED_DAP_INDIA_READINESS";
const AGENT_4B = "agent_4b_extended_dap";

export async function saveArtifact(input) {
  requireRuntimeConfig();
  const parsed = parseOrThrow(saveArtifactSchema, input);
  assertRunId(parsed.run_id);
  assertKnownPhase(parsed.phase);
  assertKnownArtifactName(parsed.artifact_name);
  assertCanWriteArtifact(parsed.agent_id, parsed.artifact_name);
  assertPhaseCanWriteArtifact(parsed.phase, parsed.artifact_name);
  await assertArtifactSaveOrder(parsed);
  const run = await getRunRecord(parsed.run_id);
  const version = await getNextArtifactVersion(parsed.run_id, parsed.artifact_name);
  const driveResult = await saveJsonArtifactToDrive({ run_id: parsed.run_id, artifact_name: parsed.artifact_name, version, drive_folder_id: run.drive_folder_id, artifact: parsed.artifact });
  const meta = await saveArtifactMetadata({ run_id: parsed.run_id, artifact_name: parsed.artifact_name, phase: parsed.phase, agent_id: parsed.agent_id, lock_status: parsed.lock_status, version, drive_file_id: driveResult.drive_file_id, drive_web_view_link: driveResult.drive_web_view_link, drive_folder_id: run.drive_folder_id, artifact_size_bytes: driveResult.artifact_size_bytes });
  if (parsed.artifact_name === "data_provenance_profile_forensics" && ACCEPTED_PHASE_STATUSES.has(parsed.lock_status)) await saveExtendedDapIndiaReadiness({ run, dataForensicsArtifact: parsed.artifact });
  await updateRunRecord(parsed.run_id, { current_phase: parsed.phase, status: parsed.lock_status });
  await logEvent({ run_id: parsed.run_id, event_type: "ARTIFACT_SAVED", actor: parsed.agent_id, payload: { phase: parsed.phase, artifact_name: parsed.artifact_name, version, lock_status: parsed.lock_status, save_order_gate: "PASS" } });
  return { ok: true, run_id: parsed.run_id, artifact_name: parsed.artifact_name, version, lock_status: parsed.lock_status, drive_file_id: meta.drive_file_id, drive_web_view_link: meta.drive_web_view_link, receipt: `${parsed.artifact_name}_v${version} saved for ${parsed.run_id}` };
}

async function saveExtendedDapIndiaReadiness({ run, dataForensicsArtifact }) {
  const upstreamNames = ["source_discovery_handoff", "legal_cartography_index", "target_profile", "target_profile_forensics", "target_feature_profile", "target_feature_profile_forensics", "data_provenance_profile"];
  const artifacts = { data_provenance_profile_forensics: dataForensicsArtifact };
  for (const name of upstreamNames) { try { artifacts[name] = await readInternalArtifactPayload(run.run_id, name); } catch (_error) { artifacts[name] = null; } }
  const output = buildExtendedDapIndiaReadinessProfile({ run, artifacts });
  const artifact = output?.[AGENT_4B_ARTIFACT];
  if (!artifact || typeof artifact !== "object") throw new Error(`AGENT4B_OUTPUT_MISSING:${AGENT_4B_ARTIFACT}`);
  const lock_status = ["LOCKED", "LOCKED_WITH_LIMITATIONS", "REPAIR_REQUIRED", "CONTROLLED_FAILURE"].includes(artifact.lock_status || artifact.status) ? (artifact.lock_status || artifact.status) : "LOCKED_WITH_LIMITATIONS";
  const version = await getNextArtifactVersion(run.run_id, AGENT_4B_ARTIFACT);
  const driveResult = await saveJsonArtifactToDrive({ run_id: run.run_id, artifact_name: AGENT_4B_ARTIFACT, version, drive_folder_id: run.drive_folder_id, artifact });
  await saveArtifactMetadata({ run_id: run.run_id, artifact_name: AGENT_4B_ARTIFACT, phase: AGENT_4B_PHASE, agent_id: AGENT_4B, lock_status, version, drive_file_id: driveResult.drive_file_id, drive_web_view_link: driveResult.drive_web_view_link, drive_folder_id: run.drive_folder_id, artifact_size_bytes: driveResult.artifact_size_bytes });
  await logEvent({ run_id: run.run_id, event_type: "AGENT4B_EXTENDED_DAP_COMPLETED", actor: AGENT_4B, payload: { artifact_name: AGENT_4B_ARTIFACT, lock_status, field_count: artifact.field_count, model_usage: "NONE_DETERMINISTIC" } });
}

async function assertArtifactSaveOrder(parsed) {
  const { run_id, phase, artifact_name } = parsed;
  if (artifact_name === "target_profile_forensics") await requireSavedArtifact(run_id, "target_profile", "SAVE_ORDER_BLOCKED:target_profile_forensics_requires_target_profile");
  if (artifact_name === "target_feature_profile") { await requireSavedArtifact(run_id, "target_profile", "SAVE_ORDER_BLOCKED:target_feature_profile_requires_target_profile"); await requireSavedArtifact(run_id, "target_profile_forensics", "SAVE_ORDER_BLOCKED:target_feature_profile_requires_target_profile_forensics"); await requirePhaseAccepted(run_id, "target_profile_forensics", "SAVE_ORDER_BLOCKED:target_feature_profile_requires_locked_m7"); }
  if (artifact_name === "target_feature_profile_forensics") { await requireSavedArtifact(run_id, "target_profile", "SAVE_ORDER_BLOCKED:target_feature_profile_forensics_requires_target_profile"); await requireSavedArtifact(run_id, "target_profile_forensics", "SAVE_ORDER_BLOCKED:target_feature_profile_forensics_requires_target_profile_forensics"); await requirePhaseAccepted(run_id, "target_profile_forensics", "SAVE_ORDER_BLOCKED:target_feature_profile_forensics_requires_locked_m7"); await requireSavedArtifact(run_id, "target_feature_profile", "SAVE_ORDER_BLOCKED:target_feature_profile_forensics_requires_target_feature_profile"); }
  if (artifact_name === "data_provenance_profile") { await requireSavedArtifact(run_id, "target_feature_profile", "SAVE_ORDER_BLOCKED:data_provenance_requires_target_feature_profile"); await requireSavedArtifact(run_id, "target_feature_profile_forensics", "SAVE_ORDER_BLOCKED:data_provenance_requires_target_feature_profile_forensics"); await requirePhaseAccepted(run_id, "target_feature_profile_forensics", "SAVE_ORDER_BLOCKED:data_provenance_requires_locked_m8"); }
  if (artifact_name === "data_provenance_profile_forensics") { await requireSavedArtifact(run_id, "data_provenance_profile", "SAVE_ORDER_BLOCKED:data_provenance_forensics_requires_data_provenance_profile"); await requirePhaseAccepted(run_id, "data_provenance_profile", "SAVE_ORDER_BLOCKED:data_provenance_forensics_requires_locked_m10_material"); }
  if (artifact_name === "exposure_registry_route_plan") { await requireSavedArtifact(run_id, "data_provenance_profile", "SAVE_ORDER_BLOCKED:m11_route_plan_requires_data_provenance_profile"); await requireSavedArtifact(run_id, "data_provenance_profile_forensics", "SAVE_ORDER_BLOCKED:m11_route_plan_requires_data_provenance_profile_forensics"); await requirePhaseAccepted(run_id, "data_provenance_profile_forensics", "SAVE_ORDER_BLOCKED:m11_route_plan_requires_locked_m10_forensics"); }
  if (M11_BATCH_VALIDATION_ARTIFACT_PATTERN.test(artifact_name)) await requireSavedArtifact(run_id, "exposure_registry_route_plan", "SAVE_ORDER_BLOCKED:m11_batch_validation_requires_route_plan");
  if (M11_BATCH_ARTIFACT_PATTERN.test(artifact_name)) { await requireSavedArtifact(run_id, "exposure_registry_route_plan", "SAVE_ORDER_BLOCKED:m11_batch_requires_route_plan"); await requireSavedArtifact(run_id, pairedBatchValidationName(artifact_name), "SAVE_ORDER_BLOCKED:m11_batch_requires_paired_m12_batch_validation"); }
  if (artifact_name === "exposure_registry_workpad_98") { await requireSavedArtifact(run_id, "exposure_registry_route_plan", "SAVE_ORDER_BLOCKED:m11_workpad_requires_route_plan"); await requireAllPlannedM11BatchesSaved(run_id, "SAVE_ORDER_BLOCKED:m11_workpad_requires_all_batches_and_validations"); }
  if (artifact_name === "exposure_registry_controlled_profile") await requireSavedArtifact(run_id, "exposure_registry_workpad_98", "SAVE_ORDER_BLOCKED:controlled_profile_requires_workpad_98");
  if (artifact_name === "exposure_registry_triggered_profile") { await requireSavedArtifact(run_id, "exposure_registry_workpad_98", "SAVE_ORDER_BLOCKED:triggered_profile_requires_workpad_98"); await requireSavedArtifact(run_id, "exposure_registry_controlled_profile", "SAVE_ORDER_BLOCKED:triggered_profile_requires_controlled_profile_first"); }
  if (artifact_name === "exposure_registry_profile_forensics") { await requireSavedArtifact(run_id, "exposure_registry_route_plan", "SAVE_ORDER_BLOCKED:m11_forensics_requires_route_plan"); await requireAllPlannedM11BatchesSaved(run_id, "SAVE_ORDER_BLOCKED:m11_forensics_requires_all_batches_and_validations"); await requireSavedArtifact(run_id, "exposure_registry_workpad_98", "SAVE_ORDER_BLOCKED:m11_forensics_requires_workpad_98"); await requireSavedArtifact(run_id, "exposure_registry_controlled_profile", "SAVE_ORDER_BLOCKED:m11_forensics_requires_controlled_profile"); await requireSavedArtifact(run_id, "exposure_registry_triggered_profile", "SAVE_ORDER_BLOCKED:m11_forensics_requires_triggered_profile"); }
  if (artifact_name === "challenge_gate") { await requireSavedArtifact(run_id, "exposure_registry_route_plan", "SAVE_ORDER_BLOCKED:challenge_gate_requires_route_plan"); await requireAllPlannedM11BatchesSaved(run_id, "SAVE_ORDER_BLOCKED:challenge_gate_requires_all_batches_and_validations"); await requireSavedArtifact(run_id, "exposure_registry_workpad_98", "SAVE_ORDER_BLOCKED:challenge_gate_requires_workpad_98"); await requireSavedArtifact(run_id, "exposure_registry_controlled_profile", "SAVE_ORDER_BLOCKED:challenge_gate_requires_controlled_profile"); await requireSavedArtifact(run_id, "exposure_registry_triggered_profile", "SAVE_ORDER_BLOCKED:challenge_gate_requires_triggered_profile"); await requireSavedArtifact(run_id, "exposure_registry_profile_forensics", "SAVE_ORDER_BLOCKED:challenge_gate_requires_m11_forensics"); await requirePhaseAccepted(run_id, "exposure_registry_profile_forensics", "SAVE_ORDER_BLOCKED:challenge_gate_requires_locked_m11_forensics"); }
  if (phase === "M7_TARGET_PROFILE" && !["target_profile", "target_profile_forensics"].includes(artifact_name)) throw new Error(`PHASE_WRITE_FORBIDDEN:${phase}:${artifact_name}`);
  if (phase === "M8_TARGET_FEATURE_PROFILE" && !["target_feature_profile", "target_feature_profile_forensics"].includes(artifact_name)) throw new Error(`PHASE_WRITE_FORBIDDEN:${phase}:${artifact_name}`);
}
async function requireAllPlannedM11BatchesSaved(runId, message) { const routePlan = await readInternalArtifactPayload(runId, "exposure_registry_route_plan"); const batches = routePlan?.batch_plan || routePlan?.exposure_registry_route_plan?.batch_plan || []; if (!Array.isArray(batches)) throw new Error(`${message}:batch_plan_missing_or_not_array`); for (const batch of batches) { if (!batch?.batch_id) throw new Error(`${message}:batch_id_missing`); await requireSavedArtifact(runId, `exposure_registry_batch_validation__${batch.batch_id}`, `${message}:missing_validation:${batch.batch_id}`); await requireSavedArtifact(runId, `exposure_registry_batch__${batch.batch_id}`, `${message}:missing_batch:${batch.batch_id}`); } }
function pairedBatchValidationName(batchArtifactName) { return batchArtifactName.replace(/^exposure_registry_batch__/, "exposure_registry_batch_validation__"); }
async function readInternalArtifactPayload(runId, artifactName) { const meta = await getArtifactMetadata(runId, artifactName); return readJsonArtifactFromDrive(meta.drive_file_id); }
async function requireSavedArtifact(runId, artifactName, message) { try { const meta = await getArtifactMetadata(runId, artifactName); if (DETERMINISTIC_PROFILE_FORENSICS.has(artifactName)) await assertForensicsNotStale(runId, artifactName, meta, message); } catch (_error) { throw new Error(message); } }
async function requirePhaseAccepted(runId, artifactName, message) { let meta; try { meta = await getArtifactMetadata(runId, artifactName); if (DETERMINISTIC_PROFILE_FORENSICS.has(artifactName)) await assertForensicsNotStale(runId, artifactName, meta, message); } catch (_error) { throw new Error(message); } if (!ACCEPTED_PHASE_STATUSES.has(meta.lock_status)) throw new Error(`${message}:status:${meta.lock_status || "missing"}`); }
export async function readArtifact({ run_id, artifact_name, agent_id = "operator" }) { assertRunId(run_id); if (artifact_name !== AGENT_4B_ARTIFACT) { assertKnownArtifactName(artifact_name); assertCanReadArtifact(agent_id, artifact_name); } const run = await getRunRecord(run_id); const meta = await getArtifactMetadata(run_id, artifact_name); const rawArtifact = await readJsonArtifactFromDrive(meta.drive_file_id); const artifact = await mergeUploadedDocumentSourcesIntoArtifact({ run, artifactName: artifact_name, artifact: rawArtifact }); const lock_status = DETERMINISTIC_PROFILE_FORENSICS.has(artifact_name) && isStaleForensicsArtifact(artifact_name, artifact) ? "REPAIR_REQUIRED" : meta.lock_status; return { ok: true, run_id, artifact_name, version: meta.latest_version || meta.version, lock_status, artifact }; }
export async function readArtifactPayload({ run_id, artifact_name, agent_id = "operator" }) { const result = await readArtifact({ run_id, artifact_name, agent_id }); if (DETERMINISTIC_PROFILE_FORENSICS.has(artifact_name) && result.lock_status === "REPAIR_REQUIRED") throw new Error(`STALE_FORENSICS_ARTIFACT:${artifact_name}`); return result.artifact; }
export async function listArtifacts(runId) { assertRunId(runId); return listArtifactMetadata(runId); }
export async function assertRequiredArtifactsExist(runId, artifactNames) { for (const artifactName of artifactNames) { if (String(artifactName || "").includes("{GROUP}")) continue; await getArtifactMetadata(runId, artifactName); } }
export async function lockPhase(input) { const body = parseOrThrow(lockPhaseSchema, input); assertRunId(body.run_id); assertKnownPhase(body.phase); if (body.phase === "M8_TARGET_FEATURE_PROFILE") { await requireSavedArtifact(body.run_id, "target_profile", "PHASE_LOCK_BLOCKED:M8_requires_target_profile"); await requireSavedArtifact(body.run_id, "target_profile_forensics", "PHASE_LOCK_BLOCKED:M8_requires_target_profile_forensics"); await requirePhaseAccepted(body.run_id, "target_profile_forensics", "PHASE_LOCK_BLOCKED:M8_requires_locked_m7"); } if (body.phase === "M10") { await requireSavedArtifact(body.run_id, "target_feature_profile", "PHASE_LOCK_BLOCKED:M10_requires_target_feature_profile"); await requireSavedArtifact(body.run_id, "target_feature_profile_forensics", "PHASE_LOCK_BLOCKED:M10_requires_target_feature_profile_forensics"); await requirePhaseAccepted(body.run_id, "target_feature_profile_forensics", "PHASE_LOCK_BLOCKED:M10_requires_locked_m8"); } if (body.phase === "M10_FORENSICS") { await requireSavedArtifact(body.run_id, "target_feature_profile", "PHASE_LOCK_BLOCKED:M10_FORENSICS_requires_target_feature_profile"); await requireSavedArtifact(body.run_id, "target_feature_profile_forensics", "PHASE_LOCK_BLOCKED:M10_FORENSICS_requires_target_feature_profile_forensics"); await requirePhaseAccepted(body.run_id, "target_feature_profile_forensics", "PHASE_LOCK_BLOCKED:M10_FORENSICS_requires_locked_m8"); await requireSavedArtifact(body.run_id, "data_provenance_profile", "PHASE_LOCK_BLOCKED:M10_FORENSICS_requires_data_provenance_profile"); await requirePhaseAccepted(body.run_id, "data_provenance_profile", "PHASE_LOCK_BLOCKED:M10_FORENSICS_requires_locked_m10_material"); } if (body.phase === "M11" && LOCK_ADVANCE_STATUSES.has(body.status)) { await requireSavedArtifact(body.run_id, "exposure_registry_route_plan", "PHASE_LOCK_BLOCKED:M11_requires_route_plan"); await requireAllPlannedM11BatchesSaved(body.run_id, "PHASE_LOCK_BLOCKED:M11_requires_all_batches_and_validations"); await requireSavedArtifact(body.run_id, "exposure_registry_workpad_98", "PHASE_LOCK_BLOCKED:M11_requires_workpad_98"); await requireSavedArtifact(body.run_id, "exposure_registry_controlled_profile", "PHASE_LOCK_BLOCKED:M11_requires_controlled_profile"); await requireSavedArtifact(body.run_id, "exposure_registry_triggered_profile", "PHASE_LOCK_BLOCKED:M11_requires_triggered_profile"); await requireSavedArtifact(body.run_id, "exposure_registry_profile_forensics", "PHASE_LOCK_BLOCKED:M11_requires_forensics"); await requirePhaseAccepted(body.run_id, "exposure_registry_profile_forensics", "PHASE_LOCK_BLOCKED:M11_requires_locked_forensics"); } if (body.phase === "M12" && LOCK_ADVANCE_STATUSES.has(body.status)) { await requireSavedArtifact(body.run_id, "exposure_registry_route_plan", "PHASE_LOCK_BLOCKED:M12_requires_route_plan"); await requireAllPlannedM11BatchesSaved(body.run_id, "PHASE_LOCK_BLOCKED:M12_requires_all_batches_and_validations"); await requireSavedArtifact(body.run_id, "exposure_registry_workpad_98", "PHASE_LOCK_BLOCKED:M12_requires_workpad_98"); await requireSavedArtifact(body.run_id, "exposure_registry_controlled_profile", "PHASE_LOCK_BLOCKED:M12_requires_controlled_profile"); await requireSavedArtifact(body.run_id, "exposure_registry_triggered_profile", "PHASE_LOCK_BLOCKED:M12_requires_triggered_profile"); await requireSavedArtifact(body.run_id, "exposure_registry_profile_forensics", "PHASE_LOCK_BLOCKED:M12_requires_m11_forensics"); await requirePhaseAccepted(body.run_id, "exposure_registry_profile_forensics", "PHASE_LOCK_BLOCKED:M12_requires_locked_m11_forensics"); await requireSavedArtifact(body.run_id, "challenge_gate", "PHASE_LOCK_BLOCKED:M12_requires_challenge_gate"); } if (LOCK_ADVANCE_STATUSES.has(body.status) && body.phase !== "COMPLETE") await assertRequiredArtifactsExist(body.run_id, getRequiredWritesForPhase(body.phase)); const existing = await getRunRecord(body.run_id); const patch = { current_phase: body.next_phase || body.phase, status: body.status, final_report_url: body.final_report_url || existing.final_report_url || "" }; const updated = await updateRunRecord(body.run_id, patch); await updateRunDashboardRow(updated); await logEvent({ run_id: body.run_id, event_type: "PHASE_LOCKED", actor: body.agent_id, payload: { phase: body.phase, status: body.status, next_phase: body.next_phase || null } }); return { ok: true, run_id: body.run_id, phase: body.phase, status: body.status, next_phase: body.next_phase || null }; }
async function assertForensicsNotStale(runId, artifactName, meta, message) { const artifact = await readJsonArtifactFromDrive(meta.drive_file_id); if (isStaleForensicsArtifact(artifactName, artifact)) { await logEvent({ run_id: runId, event_type: "STALE_FORENSICS_REJECTED", actor: "artifact-service", payload: { artifact_name: artifactName, version: meta.latest_version || meta.version, reason: "missing deterministic forensic trace contract" } }); throw new Error(`${message}:stale_forensics`); } }
function isStaleForensicsArtifact(artifactName, artifact) { if (artifactName === "exposure_registry_profile_forensics") return isStaleM11ForensicsArtifact(artifact); return isStaleDeterministicForensics({ artifactName, artifact }); }
function isStaleM11ForensicsArtifact(artifact) { const root = artifact?.exposure_registry_profile_forensics || artifact || {}; return root.forensic_contract?.contract_name !== M11_FORENSIC_CONTRACT || root.forensic_contract?.model_generated_forensics_allowed !== false || !Array.isArray(root.forensic_trace_index) || !Array.isArray(root.material_profile_trace_index) || root.forensic_boundary?.semantic_forensic_profile_retired !== true; }
export function buildReportUrl(runId) { const base = config.reviewerPublicBaseUrl || config.rendererBaseUrl || ""; if (!base) return ""; const clean = base.replace(/\/$/, ""); return `${clean}/interface-diligence/diligence-system/report.html?run_id=${encodeURIComponent(runId)}`; }
export function artifactSaveBody({ run_id, phase, agent_id, artifact_name, artifact, lock_status = "LOCKED" }) { return { run_id, phase, agent_id, artifact_name, lock_status, artifact }; }
