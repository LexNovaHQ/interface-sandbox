// Compatibility bridge only. Permission checks are owned by the central runtime contract.
export { assertCanReadArtifact, assertCanWriteArtifact, publicPermissionMatrix } from "./runtime/contracts/artifact-permissions.contract.js";
