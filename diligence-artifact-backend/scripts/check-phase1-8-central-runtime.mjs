import assert from "node:assert/strict";
import fs from "node:fs";
import path from "node:path";
import { PIPELINE_CONTRACTS, INTERNAL_PIPELINE_JOB_IDS, PIPELINE_CONTRACT_STATUS } from "../src/runtime/contracts/pipeline.contract.js";
import { CENTRAL_PHASES } from "../src/runtime/contracts/central-phase.contract.js";
import { ARTIFACT_NAMES, CARTOGRAPHY_ARTIFACT_NAMES, CARTOGRAPHY_LAYER5_ARTIFACT_NAMES, INTERNAL_JOB_WRITE_PERMISSIONS, PHASE7_DAP_BATCH_ARTIFACT_NAMES, READ_PERMISSIONS, WRITE_PERMISSIONS, SOURCE_DISCOVERY_HANDOFF_ARTIFACT_NAMES, artifactMatchesPermission } from "../src/runtime/contracts/artifact-permissions.contract.js";
import { PHASE7_DATA_PRIVACY_ARCHITECTURE_CONTRACT } from "../src/phases/07-data-provenance-profile/data-provenance-profile.contract.js";

const ROOT = process.cwd();
const read = (file) => fs.readFileSync(path.join(ROOT, file), "utf8");
const phase1To8Jobs = Object.freeze(["AGENT_1A_URL_MANIFEST", "AGENT_1B_EXTRACT", "M6_BUCKET_INDEX", "P2_SOURCE_INVENTORY_CARTOGRAPHY", "P2_LOCATOR_SPINE", "P2_PROFILE_ROUTE_MATRIX", "P2_SEMANTIC_NAVIGATION_OVERLAY", "P2_INDEX_COMPILER_VALIDATION", "M7_TARGET_PROFILE", "M7_TARGET_PROFILE_FORENSICS", "M8_FEATURE_CANDIDATE_INVENTORY", "M8_TARGET_FEATURE_PROFILE", "M8_TARGET_FEATURE_PROFILE_FORENSICS", "DATA_PROVENANCE_PROFILE_LAYER4", "DATA_PROVENANCE_PROFILE_LAYER5", "DATA_PROVENANCE_PROFILE_FORENSICS"]);
const expectedJobChain = Object.freeze([...phase1To8Jobs, "M11"]);
const retiredActiveArtifacts = Object.freeze(["data_provenance_profile", "data_provenance_profile_forensics", "extended_dap_india_readiness_profile", "integrated_dap_report", "m10_selected_legal_support_packet", "m7_deterministic_legal_signal_overlay", "legal_cartography_deterministic_map", "legal_cartography_semantic_profile", "legal_cartography_reinvestigation_workpad", "legal_cartography_index", "legal_signal_derivation_profile", "data_privacy_navigation_index"]);
const forbiddenLegacyMarkers = Object.freeze(["lossless_family__", "ROOT_FAMILY", "T0_ROOT", "P1_PRODUCT", "D1_SECURITY_TRUST", "L1_CORE_TERMS_PRIVACY"]);
const expectedValidationNames = Object.freeze(Array.from({ length: 17 }, (_, index) => `dap_semantic_batch_validation__DAP-SEM-BATCH-${String(index + 1).padStart(2, "0")}`));
const activeFiles = Object.freeze(["package.json", "server.js", "src/runtime/main.js", "src/runtime/app.js", "src/runtime/routes/operator.routes.js", "src/runtime/routes/public.routes.js", "src/runtime/services/async.service.js", "src/runtime/services/pipeline.service.js", "src/runtime/services/artifacts.service.js", "src/runtime/contracts/pipeline.contract.js", "src/runtime/contracts/central-phase.contract.js", "src/runtime/contracts/artifact-permissions.contract.js", "src/runtime/contracts/artifacts.contract.js", "src/phase-contracts.js", "src/phases/01-source-discovery/source-discovery.contract.js", "src/phases/01-source-discovery/source-discovery.runner.js", "src/phases/02-cartography-index/cartography-index.contract.js", "src/phases/02-cartography-index/cartography-index.runner.js", "src/phases/07-data-provenance-profile/data-provenance-profile.contract.js"]);
const files = Object.fromEntries(activeFiles.map((file) => [file, read(file)]));
checkProductionEntrypointCutover(files); checkLegacyReviewerStackInactive(files); checkNoLegacyPollution(files); checkPhase1To8PipelineChain(); checkDispatchText(files); checkPhase2CartographyContractSync(files); checkPhase7ContractSync(); checkArtifactPermissionSync(); checkArtifactGateSync(files); checkBlockingIsExceptionPolicy(files);
console.log(JSON.stringify({ check: "phase1-8 central runtime", status: "PASS", enforced_gates: ["PACKAGE_STARTS_CENTRAL_RUNTIME", "SERVER_JS_DELEGATES_TO_CENTRAL_RUNTIME", "NO_ACTIVE_LEGACY_REVIEWER_ADVANCE", "PHASE1_AGNOSTIC_SOURCE_DISCOVERY_NO_LEGACY_FAMILY_ARTIFACTS", "PHASE2_CARTOGRAPHY_INDEX_RUNTIME_WIRED", "PHASE1_8_DISPATCH_CONTRACT_ARTIFACT_SYNC", "PHASE7_DATA_PROVENANCE_PERMISSION_BOUNDARY", "BLOCKING_IS_EXCEPTION_LIMITATION_POLICY"] }, null, 2));
function checkProductionEntrypointCutover(files) { const pkg = JSON.parse(files["package.json"]); assert.equal(pkg.scripts?.start, "node src/runtime/main.js"); const server = files["server.js"] || ""; assert.ok(server.includes("./src/runtime/main.js")); assert.ok(server.includes("startRuntimeServer")); for (const forbidden of ["./src/reviewer-routes.js", "./src/public-reviewer-routes.js", "reviewerRouter", "publicReviewerRouter", "agent3LockPhaseRoute", "saveArtifactHandler", "M10"]) assert.ok(!server.includes(forbidden), `server.js must not expose retired runtime marker ${forbidden}`); }
function checkLegacyReviewerStackInactive(files) { const text = Object.entries(files).filter(([file]) => file.includes("runtime/") || file === "package.json" || file === "server.js").map(([, content]) => content || "").join("\n"); for (const forbidden of ["reviewer-runner-normalized.js", "reviewer-async-runner.js", "reviewer-routes.js", "public-reviewer-routes.js", "./phase-contracts.js", "from \"./phase-contracts.js\"", "from '../phase-contracts.js'"]) assert.ok(!text.includes(forbidden), `active runtime entrypoint must not import retired reviewer/phase-contract marker ${forbidden}`); }
function checkNoLegacyPollution(files) { const scanned = Object.entries(files).filter(([file]) => !file.includes("cartography-index.contract.js") && !file.includes("data-provenance-profile.contract.js")).map(([, content]) => content).join("\n"); for (const marker of forbiddenLegacyMarkers) assert.equal(scanned.includes(marker), false, `active runtime contains legacy marker ${marker}`); for (const retired of retiredActiveArtifacts) assert.equal(ARTIFACT_NAMES.includes(retired), false, `${retired} must not be active artifact`); }
function checkPhase1To8PipelineChain() { assert.deepEqual(INTERNAL_PIPELINE_JOB_IDS.slice(0, expectedJobChain.length), expectedJobChain); for (const jobId of phase1To8Jobs) assert.ok(PIPELINE_CONTRACTS[jobId], `missing pipeline contract for ${jobId}`); for (const jobId of phase1To8Jobs.slice(0, -1)) assert.equal(PIPELINE_CONTRACTS[jobId].next, expectedJobChain[expectedJobChain.indexOf(jobId) + 1], `${jobId} next mismatch`); assert.equal(PIPELINE_CONTRACTS.DATA_PROVENANCE_PROFILE_FORENSICS.next, "M11"); const centralJobs = CENTRAL_PHASES.filter((phase) => phase.sequence <= 8).flatMap((phase) => [...phase.internal_jobs]); assert.deepEqual(centralJobs, phase1To8Jobs); }
function checkDispatchText(files) { const pipeline = files["src/runtime/services/pipeline.service.js"]; for (const marker of ["runSourceUrlManifestJob", "runSourceExtractionJob", "runSourceDiscoveryHandoffJob", "runCartographyIndexRuntimeJob", "runTargetProfileReviewRuntimeJob", "runTargetProfileForensicsRuntimeJob", "runActivityCandidateInventoryRuntimeJob", "runActivityProfileReviewRuntimeJob", "runActivityProfileForensicsRuntimeJob", "runDataProvenanceProfileRuntimeJob", "runDapForensicsRuntimeJob"]) assert.ok(pipeline.includes(marker), `pipeline.service.js missing dispatch marker ${marker}`); }
function checkPhase2CartographyContractSync(files) { assert.ok(files["src/phases/02-cartography-index/cartography-index.runner.js"].includes("runCartographyIndexJob")); assert.deepEqual(CARTOGRAPHY_LAYER5_ARTIFACT_NAMES, ["target_profile_source_index", "activity_profile_source_index", "data_provenance_source_index", "legal_governance_source_index", "cartography_index", "cartography_validation_manifest"]); assert.deepEqual(PIPELINE_CONTRACTS.P2_INDEX_COMPILER_VALIDATION.writes, CARTOGRAPHY_LAYER5_ARTIFACT_NAMES); assert.equal(PIPELINE_CONTRACTS.M7_TARGET_PROFILE.reads.includes("target_profile_source_index"), true); assert.equal(PIPELINE_CONTRACTS.M8_TARGET_FEATURE_PROFILE.reads.includes("activity_profile_source_index"), true); assert.equal(PIPELINE_CONTRACTS.DATA_PROVENANCE_PROFILE_LAYER4.reads.includes("data_provenance_source_index"), true); assert.equal(PIPELINE_CONTRACTS.DATA_PROVENANCE_PROFILE_LAYER4.reads.includes("legal_governance_source_index"), true); }
function checkPhase7ContractSync() { const layer4Reads = PIPELINE_CONTRACTS.DATA_PROVENANCE_PROFILE_LAYER4.reads; for (const artifactName of ["cartography_index", "data_provenance_source_index", "legal_governance_source_index"]) assert.ok(layer4Reads.includes(artifactName), `Phase 7 must read ${artifactName}`); assert.equal(layer4Reads.includes("data_privacy_navigation_index"), false); assert.equal(layer4Reads.includes("legal_cartography_index"), false); assert.equal(layer4Reads.includes("legal_signal_derivation_profile"), false); const layer5Reads = PIPELINE_CONTRACTS.DATA_PROVENANCE_PROFILE_LAYER5.reads; assert.deepEqual(layer5Reads, ["dap_semantic_batch_route_manifest", ...PHASE7_DAP_BATCH_ARTIFACT_NAMES, ...expectedValidationNames]); assert.equal(PHASE7_DATA_PRIVACY_ARCHITECTURE_CONTRACT.blocking_is_exception_noncritical_limitations_pass, true); }
function checkArtifactPermissionSync() { for (const jobId of phase1To8Jobs) { const contract = PIPELINE_CONTRACTS[jobId]; const allowedWrites = INTERNAL_JOB_WRITE_PERMISSIONS[jobId] || []; for (const artifactName of contract.writes || []) assert.ok(allowedWrites.some((permission) => artifactMatchesPermission(artifactName, permission)), `${jobId} write ${artifactName} missing internal permission`); for (const artifactName of contract.dynamic_writes || []) assert.ok(allowedWrites.some((permission) => artifactMatchesPermission(artifactName.replace("{BATCH_ID}", "DAP-SEM-BATCH-01"), permission) || artifactMatchesPermission("legal_doc_terms_of_service", permission) || permission === artifactName), `${jobId} dynamic write ${artifactName} missing internal permission`); } for (const [agentId, permissions] of Object.entries(WRITE_PERMISSIONS)) if (agentId !== "operator") for (const retired of retiredActiveArtifacts) assert.ok(!permissions.includes(retired), `${agentId} write permissions include retired artifact ${retired}`); for (const [agentId, permissions] of Object.entries(READ_PERMISSIONS)) if (agentId !== "operator") for (const retired of retiredActiveArtifacts) assert.ok(!permissions.includes(retired), `${agentId} read permissions include retired artifact ${retired}`); for (const artifactName of CARTOGRAPHY_ARTIFACT_NAMES) assert.ok(WRITE_PERMISSIONS.agent_2_cartography_index.includes(artifactName)); for (const artifactName of SOURCE_DISCOVERY_HANDOFF_ARTIFACT_NAMES) assert.ok(INTERNAL_JOB_WRITE_PERMISSIONS.M6_BUCKET_INDEX.includes(artifactName)); }
function checkArtifactGateSync(files) { const artifactService = files["src/runtime/services/artifacts.service.js"]; for (const marker of ["cartography_source_inventory_requires_source_handoff", "locator_spine_requires_source_inventory", "profile_route_matrix_requires_locator_spine", "semantic_overlay_requires_profile_route_matrix", "target_profile_requires_cartography_index", "dap_route_requires_data_provenance_source_index", "dap_forensics_requires_data_provenance_source_index", "exposure_route_plan_requires_dap_forensics"]) assert.ok(artifactService.includes(marker), `artifacts.service.js missing gate marker ${marker}`); for (const legacy of ["extended_dap_india_readiness_profile", "integrated_dap_report", "data_provenance_profile_forensics", "data_privacy_navigation_index", "legal_signal_derivation_profile"]) assert.ok(!artifactService.includes(`ART.${legacy}`), `artifacts.service.js must not gate on ${legacy}`); }
function checkBlockingIsExceptionPolicy(files) { assert.equal(PIPELINE_CONTRACT_STATUS.blocking_is_exception_noncritical_limitations_pass, true); assert.equal(PHASE7_DATA_PRIVACY_ARCHITECTURE_CONTRACT.blocking_is_exception_noncritical_limitations_pass, true); assert.ok(files["src/phases/01-source-discovery/source-discovery.runner.js"].includes("blocking_is_exception_noncritical_limitations_pass: true")); }
