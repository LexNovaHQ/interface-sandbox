import assert from "node:assert/strict";
import { readdir, readFile } from "node:fs/promises";
import path from "node:path";
import { PIPELINE_CONTRACTS, INTERNAL_PIPELINE_JOB_IDS, PIPELINE_CONTRACT_STATUS } from "../src/runtime/contracts/pipeline.contract.js";
import { CENTRAL_PHASES } from "../src/runtime/contracts/central-phase.contract.js";
import { ARTIFACT_NAMES, ACTIVITY_PROFILE_SOURCE_ARTIFACT_NAMES, DATA_PROVENANCE_SOURCE_ARTIFACT_NAMES, INTERNAL_JOB_WRITE_PERMISSIONS, LEGAL_GOVERNANCE_SOURCE_ARTIFACT_NAMES, PHASE7_DAP_BATCH_ARTIFACT_NAMES, PHASE7_DAP_LAYER5_ARTIFACT_NAMES, READ_PERMISSIONS, SOURCE_DISCOVERY_CONTROL_ARTIFACT_NAMES, SOURCE_DISCOVERY_HANDOFF_ARTIFACT_NAMES, SOURCE_DISCOVERY_LEGAL_DOC_CONTROL_ARTIFACT_NAMES, TARGET_PROFILE_SOURCE_ARTIFACT_NAMES, WRITE_PERMISSIONS, artifactMatchesPermission } from "../src/runtime/contracts/artifact-permissions.contract.js";
import { PHASE7_DATA_PRIVACY_ARCHITECTURE_CONTRACT } from "../src/phases/07-data-provenance-profile/data-provenance-profile.contract.js";
import { ACTIVITY_PROFILE_REVIEW_CONTRACT } from "../src/phases/05-activity-profile-review/activity-profile-review.contract.js";

const root = process.cwd();
const phase1To8Jobs = Object.freeze(["AGENT_1A_URL_MANIFEST", "AGENT_1B_EXTRACT", "M6_BUCKET_INDEX", "M9", "M7_TARGET_PROFILE", "M7_TARGET_PROFILE_FORENSICS", "M8_FEATURE_CANDIDATE_INVENTORY", "M8_TARGET_FEATURE_PROFILE", "M8_TARGET_FEATURE_PROFILE_FORENSICS", "DATA_PROVENANCE_PROFILE_LAYER4", "DATA_PROVENANCE_PROFILE_LAYER5", "DATA_PROVENANCE_PROFILE_FORENSICS"]);
const expectedJobChain = Object.freeze([...phase1To8Jobs, "M11"]);
const retiredActiveArtifacts = Object.freeze(["data_provenance_profile", "data_provenance_profile_forensics", "extended_dap_india_readiness_profile", "integrated_dap_report", "m10_selected_legal_support_packet", "m7_deterministic_legal_signal_overlay"]);
const forbiddenPhase1LegacyMarkers = Object.freeze(["lossless_family__", "ROOT_FAMILY", "T0_ROOT", "P1_PRODUCT", "D1_SECURITY_TRUST", "L1_CORE_TERMS_PRIVACY"]);
const expectedValidationNames = Object.freeze(Array.from({ length: 17 }, (_, index) => `dap_semantic_batch_validation__DAP-SEM-BATCH-${String(index + 1).padStart(2, "0")}`));
const activeEntrypointFiles = Object.freeze(["package.json", "server.js", "src/runtime/main.js", "src/runtime/app.js", "src/runtime/routes/operator.routes.js", "src/runtime/routes/public.routes.js", "src/runtime/services/async.service.js", "src/runtime/services/pipeline.service.js", "src/runtime/services/artifacts.service.js"]);

const files = await loadFilesForStaticChecks();
checkProductionEntrypointCutover(files);
checkLegacyReviewerStackInactive(files);
checkPhase1NoLegacyFamilyPollution(files);
checkPhase1To8PipelineChain();
checkPhase1To8DispatchText(files);
checkActivityProfileV4DerivationContract(files);
checkPhase7ContractSync();
checkArtifactPermissionSync();
checkArtifactGateSync(files);
checkBlockingIsExceptionPolicy(files);

console.log(JSON.stringify({ check: "phase1-8 central runtime", status: "PASS", enforced_gates: ["PACKAGE_STARTS_CENTRAL_RUNTIME", "SERVER_JS_DELEGATES_TO_CENTRAL_RUNTIME", "NO_ACTIVE_LEGACY_REVIEWER_ADVANCE", "PHASE1_AGNOSTIC_SOURCE_DISCOVERY_NO_LEGACY_FAMILY_ARTIFACTS", "PHASE1_8_DISPATCH_CONTRACT_ARTIFACT_SYNC", "ACTIVITY_PROFILE_AI_REGISTRY_KEY_V4_DIRECT_AUTHORITY", "PHASE7_AGENT_PACKAGE_PROMPT_WIRING_SYNC", "PHASE7_DATA_PROVENANCE_PERMISSION_BOUNDARY", "BLOCKING_IS_EXCEPTION_LIMITATION_POLICY"] }, null, 2));

async function loadFilesForStaticChecks() {
  const paths = ["package.json", "server.js", "src/runtime/main.js", "src/runtime/app.js", "src/runtime/routes/operator.routes.js", "src/runtime/routes/public.routes.js", "src/runtime/services/async.service.js", "src/runtime/services/pipeline.service.js", "src/runtime/services/artifacts.service.js", "src/runtime/contracts/pipeline.contract.js", "src/runtime/contracts/central-phase.contract.js", "src/runtime/contracts/artifact-permissions.contract.js", "src/runtime/contracts/artifacts.contract.js", "src/phases/01-source-discovery/source-discovery.contract.js", "src/phases/01-source-discovery/source-discovery.runner.js", "src/phases/01-source-discovery/services/url-manifest.service.js", "src/phases/01-source-discovery/services/source-extraction.service.js", "src/phases/01-source-discovery/services/source-family-handoff.service.js", "src/phases/01-source-discovery/services/source-discovery-taxonomy.service.js", "src/phases/02-legal-cartography-index/legal-cartography-index.runner.js", "src/phases/02-legal-cartography-index/validators/legal-cartography-index.validator.js", "src/phases/05-activity-profile-review/activity-profile-review.contract.js", "src/phases/05-activity-profile-review/validators/activity-profile-review.validator.js", "src/phases/05-activity-profile-review/services/activity-candidate-inventory-index.builder.js", "src/phases/07-data-provenance-profile/data-provenance-profile.contract.js", "src/report-normalization-map.js", "agent-packages/agent_3_target_feature/03_M8_FEATURE_PROFILE_BACKEND_CURRENT.md", "agent-packages/agent_3_target_feature/AGENT3_BACKEND_OUTPUT_CONTRACT.md", "agent-packages/agent_3_target_feature/AGENT3_RUNTIME_BINDING_PACKET.yaml", "references/registry/REFERENCE_MANIFEST.json", ...(await listFiles("src/phases", ".js"))];
  const unique = [...new Set(paths.map(normalizeRepoPath))].sort();
  return Object.fromEntries(await Promise.all(unique.map(async (relativePath) => [relativePath, await readFile(resolveRepoPath(relativePath), "utf8")])));
}
async function listFiles(directory, extension) { const normalizedDirectory = normalizeRepoPath(directory); const entries = await readdir(resolveRepoPath(normalizedDirectory), { withFileTypes: true }); const out = []; for (const entry of entries) { const relativePath = `${normalizedDirectory}/${entry.name}`; if (entry.isDirectory()) out.push(...(await listFiles(relativePath, extension))); else if (entry.isFile() && entry.name.endsWith(extension)) out.push(relativePath); } return out.map(normalizeRepoPath).sort(); }
function resolveRepoPath(relativePath) { return path.join(root, ...normalizeRepoPath(relativePath).split("/")); }
function normalizeRepoPath(relativePath) { return String(relativePath || "").replaceAll("\\", "/"); }

function checkProductionEntrypointCutover(files) { const pkg = JSON.parse(files["package.json"]); assert.equal(pkg.scripts?.start, "node src/runtime/main.js"); const server = files["server.js"] || ""; assert.ok(server.includes("./src/runtime/main.js")); assert.ok(server.includes("startRuntimeServer")); for (const forbidden of ["./src/reviewer-routes.js", "./src/public-reviewer-routes.js", "reviewerRouter", "publicReviewerRouter", "agent3LockPhaseRoute", "saveArtifactHandler", "M10"]) assert.ok(!server.includes(forbidden), `server.js must not expose retired runtime marker ${forbidden}`); }
function checkLegacyReviewerStackInactive(files) { const text = activeEntrypointFiles.map((file) => files[file] || "").join("\n"); for (const forbidden of ["reviewer-runner-normalized.js", "reviewer-async-runner.js", "reviewer-routes.js", "public-reviewer-routes.js", "./phase-contracts.js", "from \"./phase-contracts.js\"", "from '../phase-contracts.js'"]) assert.ok(!text.includes(forbidden), `active runtime entrypoint must not import retired reviewer/phase-contract marker ${forbidden}`); }
function checkPhase1NoLegacyFamilyPollution(files) { const scanned = ["src/runtime/contracts/pipeline.contract.js", "src/runtime/contracts/artifact-permissions.contract.js", "src/runtime/contracts/artifacts.contract.js", "src/phases/01-source-discovery/source-discovery.contract.js", "src/phases/01-source-discovery/services/url-manifest.service.js", "src/phases/01-source-discovery/services/source-extraction.service.js", "src/phases/01-source-discovery/services/source-family-handoff.service.js", "src/phase-contracts.js", "src/phases/05-activity-profile-review/services/activity-candidate-inventory-index.builder.js"]; for (const file of scanned) for (const marker of forbiddenPhase1LegacyMarkers) assert.equal((files[file] || "").includes(marker), false, `${file} contains legacy Phase 1 marker ${marker}`); for (const retired of retiredActiveArtifacts) assert.equal(ARTIFACT_NAMES.includes(retired), false, `${retired} must not be active artifact`); }
function checkPhase1To8PipelineChain() { assert.deepEqual(INTERNAL_PIPELINE_JOB_IDS.slice(0, expectedJobChain.length), expectedJobChain); for (const jobId of phase1To8Jobs) assert.ok(PIPELINE_CONTRACTS[jobId], `missing pipeline contract for ${jobId}`); for (const jobId of phase1To8Jobs.slice(0, -1)) assert.equal(PIPELINE_CONTRACTS[jobId].next, expectedJobChain[expectedJobChain.indexOf(jobId) + 1], `${jobId} next mismatch`); assert.equal(PIPELINE_CONTRACTS.DATA_PROVENANCE_PROFILE_FORENSICS.next, "M11"); const centralJobs = CENTRAL_PHASES.filter((phase) => phase.sequence <= 8).flatMap((phase) => [...phase.internal_jobs]); assert.deepEqual(centralJobs, phase1To8Jobs); }
function checkPhase1To8DispatchText(files) { const pipeline = files["src/runtime/services/pipeline.service.js"]; for (const marker of ["runSourceUrlManifestJob", "runSourceExtractionJob", "runSourceDiscoveryHandoffJob", "runLegalCartographyIndexJob", "runTargetProfileReviewRuntimeJob", "runTargetProfileForensicsRuntimeJob", "runActivityCandidateInventoryRuntimeJob", "runActivityProfileReviewRuntimeJob", "runActivityProfileForensicsRuntimeJob", "runDataProvenanceProfileRuntimeJob", "runDapForensicsRuntimeJob"]) assert.ok(pipeline.includes(marker), `pipeline.service.js missing dispatch marker ${marker}`); }
function checkActivityProfileV4DerivationContract(files) { const activity = ACTIVITY_PROFILE_REVIEW_CONTRACT; const pipelineActivity = PIPELINE_CONTRACTS.M8_TARGET_FEATURE_PROFILE; const prompt = files["agent-packages/agent_3_target_feature/03_M8_FEATURE_PROFILE_BACKEND_CURRENT.md"]; const backendContract = files["agent-packages/agent_3_target_feature/AGENT3_BACKEND_OUTPUT_CONTRACT.md"]; const binding = files["agent-packages/agent_3_target_feature/AGENT3_RUNTIME_BINDING_PACKET.yaml"]; const validator = files["src/phases/05-activity-profile-review/validators/activity-profile-review.validator.js"]; const reportMap = files["src/report-normalization-map.js"]; const manifest = JSON.parse(files["references/registry/REFERENCE_MANIFEST.json"]); assert.equal(activity.source_authority.base_registry_key_reference, "AI_REGISTRY_KEY.md"); assert.equal(activity.source_authority.classification_matrix_active_for_material_derivation, false); assert.deepEqual(pipelineActivity.references, ["AI_REGISTRY_KEY.md", "FIELD_DERIVATION_REGISTRY_v2_LOCKED.yaml", "FORENSIC_ANNEXURE_REGISTRY_v1_LOCKED.yaml"]); for (const code of ["UNI", "DOE", "JDG", "CMP", "CRT", "RDR", "ORC", "TRN", "SHD", "OPT", "MOV", "CUR", "MOD", "ORA"]) { assert.ok(activity.output_contract.archetype_codes.includes(code)); assert.ok(prompt.includes(`${code} —`)); assert.ok(reportMap.includes(`${code}:`)); } assert.ok(validator.includes("function validateBasisCoverage")); assert.ok(backendContract.includes("Every selected archetype code must have exactly one matching")); assert.ok(binding.includes("Activity Profile Review must use AI_REGISTRY_KEY.md directly")); assert.equal(manifest.validation?.["AI_REGISTRY_KEY.md"]?.activity_profile_material_archetype_enum, "v4_14_codes"); }
function checkPhase7ContractSync() { const layer4Reads = PIPELINE_CONTRACTS.DATA_PROVENANCE_PROFILE_LAYER4.reads; for (const artifactName of DATA_PROVENANCE_SOURCE_ARTIFACT_NAMES) assert.ok(layer4Reads.includes(artifactName), `Phase 7 must read source artifact ${artifactName}`); assert.ok(layer4Reads.includes("legal_doc_{DOC_TYPE}")); assert.ok(layer4Reads.includes("legal_cartography_index")); assert.ok(layer4Reads.includes("legal_signal_derivation_profile")); const layer5Reads = PIPELINE_CONTRACTS.DATA_PROVENANCE_PROFILE_LAYER5.reads; assert.deepEqual(layer5Reads, ["dap_semantic_batch_route_manifest", ...PHASE7_DAP_BATCH_ARTIFACT_NAMES, ...expectedValidationNames]); assert.equal(PHASE7_DATA_PRIVACY_ARCHITECTURE_CONTRACT.blocking_is_exception_noncritical_limitations_pass, true); }
function checkArtifactPermissionSync() { for (const jobId of phase1To8Jobs) { const contract = PIPELINE_CONTRACTS[jobId]; const allowedWrites = INTERNAL_JOB_WRITE_PERMISSIONS[jobId] || []; for (const artifactName of contract.writes || []) assert.ok(allowedWrites.some((permission) => artifactMatchesPermission(artifactName, permission)), `${jobId} write ${artifactName} missing internal permission`); for (const artifactName of contract.dynamic_writes || []) assert.ok(allowedWrites.some((permission) => artifactMatchesPermission(artifactName.replace("{BATCH_ID}", "DAP-SEM-BATCH-01"), permission) || artifactMatchesPermission("legal_doc_terms_of_service", permission) || permission === artifactName), `${jobId} dynamic write ${artifactName} missing internal permission`); } for (const [agentId, permissions] of Object.entries(WRITE_PERMISSIONS)) if (agentId !== "operator") for (const retired of retiredActiveArtifacts) assert.ok(!permissions.includes(retired), `${agentId} write permissions include retired artifact ${retired}`); for (const [agentId, permissions] of Object.entries(READ_PERMISSIONS)) if (agentId !== "operator") for (const retired of retiredActiveArtifacts) assert.ok(!permissions.includes(retired), `${agentId} read permissions include retired artifact ${retired}`); for (const artifactName of TARGET_PROFILE_SOURCE_ARTIFACT_NAMES) assert.ok(READ_PERMISSIONS.agent_3_target_feature.includes(artifactName)); for (const artifactName of ACTIVITY_PROFILE_SOURCE_ARTIFACT_NAMES) assert.ok(READ_PERMISSIONS.agent_3_target_feature.includes(artifactName)); for (const artifactName of LEGAL_GOVERNANCE_SOURCE_ARTIFACT_NAMES) assert.ok(READ_PERMISSIONS.agent_2b_m9.includes(artifactName)); for (const artifactName of SOURCE_DISCOVERY_CONTROL_ARTIFACT_NAMES) assert.ok(READ_PERMISSIONS.agent_2a_bucket_routing.includes(artifactName) || READ_PERMISSIONS.agent_2a_bucket_routing.includes("source_discovery_matrix_manifest")); for (const artifactName of SOURCE_DISCOVERY_LEGAL_DOC_CONTROL_ARTIFACT_NAMES) assert.ok(READ_PERMISSIONS.agent_2a_bucket_routing.includes(artifactName)); for (const artifactName of SOURCE_DISCOVERY_HANDOFF_ARTIFACT_NAMES) assert.ok(INTERNAL_JOB_WRITE_PERMISSIONS.M6_BUCKET_INDEX.includes(artifactName)); }
function checkArtifactGateSync(files) { const artifactService = files["src/runtime/services/artifacts.service.js"]; for (const marker of ["dap_registry_requires_accepted_activity_forensics", "dap_route_requires_accepted_navigation_index", "dap_batch_requires_paired_validation", "dap_validation_manifest_requires_all_batches_and_validations", "dap_gate_requires_accepted_validation_manifest", "dap_forensics_requires_semantic_batch_gate", "exposure_route_plan_requires_accepted_dap_forensics"]) assert.ok(artifactService.includes(marker), `artifacts.service.js missing gate marker ${marker}`); for (const legacy of ["extended_dap_india_readiness_profile", "integrated_dap_report", "data_provenance_profile_forensics"]) assert.ok(!artifactService.includes(`ART.${legacy}`), `artifacts.service.js must not gate on ${legacy}`); }
function checkBlockingIsExceptionPolicy(files) { assert.equal(PIPELINE_CONTRACT_STATUS.blocking_is_exception_noncritical_limitations_pass, true); assert.equal(PHASE7_DATA_PRIVACY_ARCHITECTURE_CONTRACT.blocking_is_exception_noncritical_limitations_pass, true); assert.ok(files["src/phases/01-source-discovery/source-discovery.runner.js"].includes("blocking_is_exception_noncritical_limitations_pass: true")); const m9Validator = files["src/phases/02-legal-cartography-index/validators/legal-cartography-index.validator.js"]; assert.ok(m9Validator.includes("LOCKED_WITH_LIMITATIONS")); assert.ok(m9Validator.includes("throw error")); }
